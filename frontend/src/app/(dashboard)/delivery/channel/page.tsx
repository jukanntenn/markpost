import { buildPageMetadata } from "@/lib/metadata";
export const generateMetadata = buildPageMetadata("deliveryChannelDetail");
export { default } from "@/components/delivery/DeliveryChannelDetailPage";
