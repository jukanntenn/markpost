services:
  markpost:
    image: 192.168.5.50:5000/markpost:latest
    container_name: markpost
    ports:
      - "8089:7330"
    volumes:
      - ./config.toml:/app/config.toml
      - ./data:/app/data
    restart: always
