{
  "name": "markpost-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "docker compose up -d --wait && playwright test; EXIT=$?; docker compose down; exit $EXIT",
    "test:local": "BASE_URL=https://localhost:2053 BACKEND_URL=https://localhost:2053 playwright test",
    "test:local:serial": "BASE_URL=https://localhost:2053 BACKEND_URL=https://localhost:2053 playwright test --workers=1",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "@playwright/test": "1.53.0"
  }
}
